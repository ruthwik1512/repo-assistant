"""
repo_assistant package

This is the top-level package for the AI-powered repository assistant.
Sub-packages will be added here as the project grows:
  - traversal: clone and walk repositories
  - indexer: read source files and produce IndexedFile objects
  - (future) embedder: generate vector embeddings
  - (future) chat: query the indexed codebase
"""
